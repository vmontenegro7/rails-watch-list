<h1>Here are the movies lists</h1>
<div class= 'container'>
  <ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
  </ul>

  <%= link_to "Add a new list", new_list_path %>
  </div>
